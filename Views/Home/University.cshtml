@model IEnumerable<WebApplicationDatabaseProject.university>

@{
    ViewBag.Title = "University";
}

<h2>University</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.university_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.university_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.university_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.university_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.university_id })
        </td>
    </tr>
}

</table>
