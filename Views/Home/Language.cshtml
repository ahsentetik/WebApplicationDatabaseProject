@model IEnumerable<WebApplicationDatabaseProject.language>

@{
    ViewBag.Title = "Language";
}

<h2>Language</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.language1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.language1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.language_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.language_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.language_id })
        </td>
    </tr>
}

</table>
